# VotaLiveSL – versione evento

Versione attuale dell’app per il “Processo all’Intelligenza Artificiale”.

Include: votazioni live, Word Cloud con 3 parole, scaletta evento, storico, schermo risultati senza scroll e banner orizzontale.

Backend Firebase invariato (`meter-42451`): non rinominare o modificare `firebase-config.js` solo perché il frontend ora è pubblicato come VotaLiveSL.
